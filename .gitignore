# dependencies
node_modules/

# next.js
.next/
out/

# env — never commit secrets (OpenAI, Upstash, etc.)
.env
.env*.local

# misc
.DS_Store
*.log

# Local-only security backlog — do not push to shared remotes
SECURITY_IMPROVEMENTS.md
