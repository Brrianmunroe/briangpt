# Workspace Instructions

## Browser usage

- Do not open, navigate, inspect, click, or test the website in a browser unless the user explicitly requests browser use for the current task.
- Ambient in-app browser context does not grant permission to operate the browser.
- Prefer code inspection, automated tests, builds, and command-line verification by default.
- After making changes, report that the preview is ready rather than opening it automatically.
